* Globals for data work

* File paths

* root: 	path to folder where raw data extracted
* progs: 	path to do files

global progs = "~/research/seur/data" 
global root = "~/data/sipp" 		

global dir_samples "${root}/samples"
global dir_bootstraps "${root}/bootstrap"

* Matlab command (to call matlab from do-file)
global matlab_command /apps/matlab20a/bin/matlab 
